export enum TokenSlugEnums {
  LIVEPEER = "livepeer",
  MATIC = "matic",
  GRAPH = "graph",
}

export enum TokenNameEnums {
  LIVEPEER = "Livepeer",
  MATIC = "Polygon",
  GRAPH = "The Graph",
}

export enum TokenSymbolEnums {
  LIVEPEER = "LPT",
  MATIC = "MATIC",
  GRAPH = "GRT",
}

export enum ActionEnums {
  STAKE = "STAKE",
  UNSTAKE = "UNSTAKE",
  SWAP = "SWAP",
}

export enum Currency {
  USD = "usd",
  EUR = "eur",
}
